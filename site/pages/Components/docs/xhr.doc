/**
 * @title 基本功能
 */
import xhr from 'earth-ui/xhr'
import Button from 'earth-ui/Button'

const MessageSuccess = () => {
  return (
    <Button onClick={() => xhr({
      type: 'DELETE',
      url: '/user/5',
      success(data) {
        console.log(data)
      }
    })}>删除</Button>
  )
}

@component xhr