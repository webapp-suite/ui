/**
 * @title Basic Usage
 * @desc Assign any of the icon classnames to an i element to get an icon. Note that we like to reserve i for icons, so if you need to emphasize text, consider using `em`.
 */
import Icon from 'earth-ui/lib/Icon'

const IconBasic = () => {
  return <Icon type="myapps" />
}

@component Icon
